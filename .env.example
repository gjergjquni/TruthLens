# TruthLens – copy to .env and fill in values
# Backend (backend/.env)
# DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/truthlens?schema=public"
# PORT=4000
# NODE_ENV=development

# Frontend talks to backend via Vite proxy (see frontend/vite.config.ts)
# No frontend env vars required for local dev.

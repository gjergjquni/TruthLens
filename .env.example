# TruthLens – copy to .env and fill in values
# Backend (backend/.env)
# DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/truthlens?schema=public"
# PORT=4000
# NODE_ENV=development

# Frontend (frontend/.env) – optional
# VITE_N8N_WEBHOOK_URL – n8n webhook URL. In dev, Vite proxies /webhook to localhost:5678.
#   Leave unset to use /webhook/truthlens (proxied). For production use full URL (e.g. https://your-n8n.com/webhook/truthlens).

# Database (SQLite – no install needed; file is created automatically)
DATABASE_URL="file:./dev.db"

# Server
PORT=4000

# Optional: set to "development" for verbose Prisma logs
NODE_ENV=development
